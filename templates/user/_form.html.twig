{{ form_start(form) }}
<div class="eventForm">{{ form_row(form.email, {'attr': {'placeholder': 'Titre du spectacle'}}) }}</div>
<div class="eventForm">{{ form_row(form.nom) }}</div>
<div class="eventForm">{{ form_row(form.prenom, {'attr': {'placeholder': 'Lieu du spectacle'}}) }}</div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
