{% extends "base.html.twig" %}
{% block title %}
	{{evenement.titre}}
{% endblock %}

{% block body %}
	<div class="carteSavoir">
	
		<div class="imgSavoir" id="tv">
			<img src="/dossierIamge/{{evenement.image}}" alt="img" style="width:100%">
		</div>
		<div>
			<h1 class="titreSavoir">{{evenement.titre}}</h1>
			<p class="lieuSavoir">Lieu de la représentation : {{evenement.lieu}}</p>
			<p>{{evenement.description}}</p>
			<h4>Prix du spectacle : {{evenement.prix}} €</h4>
			<h4>Le spectacle aura lieu le {{evenement.dateEvenement|date('d m Y')}} à {{evenement.dateEvenement|date('H:i')}}</h4>
			<h5> Ce spectacle est du genre : {{evenement.categorie}}</h5>
		</div>
		<br>
		{# <a href="{{path('editJoueur',{'id':joueur.id})}}">
				<button type="button" class="btn btn-outline-primary">Modifier le Profil</button>
					</a>
					<br>
				<a href="{{path('deletejoueur',{'id':joueur.id})}}">
					<button type="button" class="btn btn-outline-danger">Supprimer le Profil</button>
				</a>
				<br> #}
	</div>
{% endblock %}
