{% extends "base.html.twig" %}

{% block body %}


</div>
<!-- End -->
<div class="flexy">
    <!-- Sidenav -->
    <div class="sidenav">
        <div class="profile">
            <i class="fa-solid fa-user-astronaut tt"></i>
            {# <i class="fa-solid fa-address-card"></i> #}
            {# <img src="https://imdezcode.files.wordpress.com/2020/02/imdezcode-logo.png" alt="" width="100"
                height="100"> #}
            <div class="name">
                {% if utilisateur.nom == null %}
                Nom: --
                {% else %}
                Nom: {{utilisateur.nom}} {{utilisateur.prenom}}
                {% endif %}


                {# {{"email : ".app.user.email}} #}
            </div>

            <div class="name">
                email: {{utilisateur.email}}
            </div>

        </div>

        <div class="sidenav-url">
            <div class="url">
                <a href="#profile" class="active">Profile</a>
                <hr align="center">
            </div>
            <div class="url">
                <a href="{{ path('eventbyuser') }}">Evenements</a>
                <hr align="center">
            </div>
            <div class="url">

                <a href="{{path('app_home')}}">
                    <i class="fa fa-sign-out-alt fa-2x"></i>
                </a>
                <hr align="center">
            </div>
        </div>
    </div>
    <!-- End -->

    <!-- Main -->
    <div class="main">

        <div class="cart" id="profile">
            <h2>Informations personels </h2>
            <br><br>
            <div class="card-body">

                <table>
                    <tbody>
                        <tr>
                            <td>Nom :</td>
                            {# <td><input type="text" id="nom" value="{{utilisateur.nom}}"> #}

                            <td>{{utilisateur.nom}}</td>
                        </tr>
                        <tr>
                            <td>Prenom :</td>
                            {# <td><input type="text" id="nom" value="{{utilisateur.nom}}"> #}

                            <td>{{utilisateur.prenom}}</td>
                        </tr>
                        <tr>
                            <td>Email</td>
                            <td>:</td>
                            <td>{{utilisateur.email}}</td>
                        </tr>
                        {# <tr>
                            <td>Address</td>
                            <td>:</td>
                            <td>Bali, Indonesia</td>
                        </tr>
                        <tr>
                            <td>Hobbies</td>
                            <td>:</td>
                            <td>Diving, Reading Book</td>
                        </tr>
                        <tr>
                            <td>Job</td>
                            <td>:</td>
                            <td>Web Developer</td>
                        </tr> #}

                    </tbody>
                </table>

            </div>
            <br><br><br>
            <div>
                <a href="{{path('app_user_edit',{'id':utilisateur.id})}}"><button type="button" class="btn btn-info"><i
                            class="fa-solid fa-marker"></i> modifier mon
                        profil
                    </button></a>

                {# <a href="{{path('app_user_delete',{'id':utilisateur.id})}}"><button type="button"
                        class="btn btn-primary"><i class="fa-solid fa-trash-can"></i> supprimer mon
                        profil</button></a> #}
            </div>

        </div>


    </div>
</div>


{% endblock %}